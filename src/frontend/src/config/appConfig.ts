// ---------------------------------------------------------------------------
// App-wide name. Change ONLY this one line to rename the app everywhere it's
// used from this file. (Some older screens still hard-code "CalcPro" in
// their own files — those will be switched over to APP_NAME one page at a
// time, same as everything else we're updating.)
// ---------------------------------------------------------------------------
export const APP_NAME = "TCalc AI";

// Bump this on every release. AboutPanel and the version-check banner both
// read from here so there's only one place to update.
export const APP_VERSION = "1.3.5";
